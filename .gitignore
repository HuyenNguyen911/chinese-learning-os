# Backup đầy đủ — track cả raw/ (giáo trình) và output/ (slide + baitap).
# Chỉ loại rác thật sự:
__pycache__/
~$*

# git worktree phụ (nested repo, không phải nội dung khóa học)
output/hsk2-worktree/
